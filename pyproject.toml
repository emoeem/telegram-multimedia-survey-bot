[project]
name = "telegram-bot-pdf-tools"
version = "0.1.0"
description = "PDF / Microsoft document import tooling for the telegram survey bot"
requires-python = ">=3.10"
dependencies = [
    "pymupdf>=1.24",
]

[tool.uv]
package = false
